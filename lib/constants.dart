import 'package:flutter/material.dart';

const kDefaultBorderRadius = 24.0;
const defaultBorderRadius =
    BorderRadius.all(Radius.circular(kDefaultBorderRadius));
const kDefaultPadding = 14.0;
const defaultPadding = EdgeInsets.all(kDefaultPadding);

const kDefaultAnimationDuration = Duration(milliseconds: 300);
const kMobileMaxWidth = 850;
const kTabletMaxWidth = 1100;
